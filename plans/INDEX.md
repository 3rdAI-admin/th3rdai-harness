# Plans Index

Track active and completed plans/efforts here so they are discoverable.

| Plan / Effort | Type | Status | Owner Agent | Notes |
|---------------|------|--------|-------------|-------|
| `native-orchestrator/` | Effort | **done** | Planner→Builder | Phases 01–04 implemented (config, sequencer, CLI, execution). 70/70 unit tests pass. Dry-run by default, opt-in `--execute` via `configs/execution.yaml`. See `plans/native-orchestrator/EFFORT.md`. |
| `improve-eval-case-examples.md` | Plan | done | — | Enhance eval case coverage with more representative scenarios (shipped in v1.0.0). |

Update this table when adding a plan or effort. Status values: `todo`, `doing`, `review`, `done`.

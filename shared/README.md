# Shared Resources

Place files here that are used across multiple stages. Examples:

- `templates/` — Document templates, email templates
- `examples/` — Sample outputs for reference
- `data/` — Datasets, spreadsheets, or reference data

Files in this folder should be referenced from stage CONTEXT.md files, not loaded automatically.
